import React from 'react';
import { 
  Database, Brain, MessageSquare, FileText, 
  Search, Cpu, Layers, AlertTriangle, 
  CheckCircle2, XCircle, FileQuestion,
  Combine, Sparkles, Filter, Zap, 
  ArrowDownUp, Target, HelpCircle, Clock, Coins,
  Scale, BarChart, Gauge, FileCheck, ClipboardCheck,
  Stethoscope, Crosshair
} from 'lucide-react';

// --- STYLING CONSTANTS (ByteByteGo Aesthetic) ---
const THEME = {
  cardBg: "bg-white",
  cardBorder: "border-slate-200",
  titleBg: "bg-[#41d8a0]",
  titleBorder: "border-[#2e9e73]",
  primaryNode: "bg-[#a6f0d4] border-[#31a67a] text-slate-900",
  secondaryNode: "bg-white border-slate-300 text-slate-700 shadow-sm",
  dashedNode: "bg-[#f8fafc] border-slate-400 border-dashed text-slate-700",
  errorNode: "bg-[#ffe4e6] border-[#f43f5e] text-slate-900",
  warningNode: "bg-[#fef3c7] border-[#d97706] text-slate-900",
  line: "#64748b"
};

// --- CORE UI COMPONENTS ---

const Card = ({ title, children, tall = false, xTall = false }) => (
  <div className={`relative ${THEME.cardBg} rounded-2xl shadow-sm border ${THEME.cardBorder} overflow-hidden flex flex-col items-center w-full ${xTall ? 'h-[650px]' : tall ? 'h-[500px]' : 'h-[400px]'} mb-6 hover:shadow-md transition-shadow`}>
    <div className={`mt-5 ${THEME.titleBg} text-slate-900 font-extrabold text-lg py-2 px-6 rounded-xl border-2 ${THEME.titleBorder} w-[85%] text-center z-20 shadow-sm tracking-tight`}>
      {title}
    </div>
    <div className="absolute inset-0 top-[60px] w-full p-4 flex items-center justify-center">
      {children}
    </div>
  </div>
);

const Node = ({ top, left, title, subtitle, icon: Icon, type = "primary", className = "", width = "w-[130px]" }) => {
  const styleClass = THEME[`${type}Node`] || THEME.primaryNode;
  return (
    <div 
      className={`absolute z-10 flex flex-col items-center justify-center p-2 rounded-lg border-2 ${width} text-center transition-transform hover:scale-105 ${styleClass} ${className}`}
      style={{ top: `${top}%`, left: `${left}%`, transform: 'translate(-50%, -50%)' }}
    >
      {Icon && <Icon className="mb-1 opacity-80" size={18} strokeWidth={2} />}
      <span className="text-[11px] font-bold leading-tight">{title}</span>
      {subtitle && <span className="text-[9px] opacity-80 mt-1 leading-tight whitespace-pre-line">{subtitle}</span>}
    </div>
  );
};

const Label = ({ top, left, text, icon: Icon, type="default", width="w-auto" }) => (
  <div 
    className={`absolute z-20 flex flex-col items-center justify-center px-3 py-1.5 rounded-full text-[10px] font-bold shadow-sm border border-slate-200 ${width} ${type === 'error' ? 'bg-red-50 text-red-600 border-red-200' : type === 'success' ? 'bg-[#e6fcf5] text-[#2e9e73] border-[#31a67a]' : type === 'warning' ? 'bg-[#fef3c7] text-[#d97706] border-[#f59e0b]' : 'bg-white text-slate-700'}`}
    style={{ top: `${top}%`, left: `${left}%`, transform: 'translate(-50%, -50%)' }}
  >
    {Icon && <Icon size={14} className={`mb-1 ${type === 'error' ? 'text-red-500' : type === 'warning' ? 'text-[#d97706]' : 'text-[#2e9e73]'}`} />}
    {text}
  </div>
);

const SvgCanvas = ({ children }) => (
  <svg className="absolute inset-0 w-full h-full z-0 pointer-events-none">
    <defs>
       <marker id="arrow" viewBox="0 0 10 10" refX="22" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
         <path d="M 0 0 L 10 5 L 0 10 z" fill={THEME.line} />
       </marker>
       <marker id="arrow-red" viewBox="0 0 10 10" refX="22" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
         <path d="M 0 0 L 10 5 L 0 10 z" fill="#ef4444" />
       </marker>
       <marker id="arrow-green" viewBox="0 0 10 10" refX="22" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
         <path d="M 0 0 L 10 5 L 0 10 z" fill="#31a67a" />
       </marker>
    </defs>
    {children}
  </svg>
);

const Connection = ({ x1, y1, x2, y2, dashed = false, arrow = true, color = THEME.line }) => {
  let marker = "none";
  if (arrow) {
    if (color === '#ef4444') marker = "url(#arrow-red)";
    else if (color === '#31a67a') marker = "url(#arrow-green)";
    else marker = "url(#arrow)";
  }
  return (
    <line 
      x1={`${x1}%`} y1={`${y1}%`} x2={`${x2}%`} y2={`${y2}%`} 
      stroke={color} strokeWidth="2" strokeDasharray={dashed ? "5,5" : "none"} 
      markerEnd={marker} 
    />
  );
};

// --- SPECIFIC DIAGRAM CARDS ---

// Card 1: The RAG Evaluation Problem
const CardProblem = () => (
  <div className="relative w-full h-full">
    <SvgCanvas>
      <Connection x1={20} y1={50} x2={40} y2={50} />
      <Connection x1={40} y1={50} x2={60} y2={50} />
      <Connection x1={60} y1={50} x2={80} y2={50} />
      
      {/* Question lines */}
      <path d="M 20 65 Q 20 80 40 80" fill="none" stroke="#f43f5e" strokeWidth="2" strokeDasharray="5,5" markerEnd="url(#arrow-red)" />
      <path d="M 80 65 Q 80 80 60 80" fill="none" stroke="#f43f5e" strokeWidth="2" strokeDasharray="5,5" markerEnd="url(#arrow-red)" />
    </SvgCanvas>

    <Node top={50} left={20} title='"Return policy?"' icon={MessageSquare} type="secondary" width="w-[110px]" />
    <Node top={50} left={40} title="Retrieve Chunks" subtitle="Did we get the right info?" icon={Search} type="primary" width="w-[120px]" />
    <Node top={50} left={60} title="Generate Answer" subtitle="Is it faithful to context?" icon={Brain} type="primary" width="w-[130px]" />
    <Node top={50} left={80} title='"Return in 30 days"' icon={FileText} type="success" width="w-[120px]" />

    <Label top={25} left={50} text="Looks correct, but how do we mathematically prove it?" icon={HelpCircle} />
    <Label top={80} left={50} text="Need to measure Hallucinations & Retrieval Quality!" icon={AlertTriangle} type="error" />
  </div>
);

// Card 2: RAGAS Framework Overview
const CardRagasOverview = () => (
  <div className="relative w-full h-full">
    <div className="absolute top-[8%] left-[25%] -translate-x-1/2 text-xs font-bold text-[#31a67a] uppercase tracking-widest text-center">Generation Metrics<br/><span className="text-[9px] lowercase text-slate-500">Evaluating the LLM Output</span></div>
    <div className="absolute top-[8%] left-[75%] -translate-x-1/2 text-xs font-bold text-blue-500 uppercase tracking-widest text-center">Retrieval Metrics<br/><span className="text-[9px] lowercase text-slate-500">Evaluating the Vector DB</span></div>
    <div className="absolute top-[15%] bottom-[5%] left-[50%] border-l-2 border-dashed border-slate-200"></div>

    <SvgCanvas>
      {/* Left side (Generation) */}
      <Connection x1={50} y1={50} x2={25} y2={35} arrow={false} />
      <Connection x1={50} y1={50} x2={25} y2={75} arrow={false} />
      
      {/* Right side (Retrieval) */}
      <Connection x1={50} y1={50} x2={75} y2={35} arrow={false} />
      <Connection x1={50} y1={50} x2={75} y2={75} arrow={false} />
    </SvgCanvas>

    {/* Center Node */}
    <Node top={50} left={50} title="RAGAS" subtitle="LLM-as-a-Judge" icon={Scale} type="primary" width="w-[110px] z-20 shadow-lg" />

    {/* Generation Nodes */}
    <Node top={35} left={25} title="1. Faithfulness" subtitle="No Hallucinations?\n(Supported by chunks?)" icon={FileCheck} type="secondary" width="w-[160px]" />
    <Node top={75} left={25} title="2. Answer Relevancy" subtitle="Answers the actual question?\n(Not off-topic?)" icon={Target} type="secondary" width="w-[160px]" />

    {/* Retrieval Nodes */}
    <Node top={35} left={75} title="3. Context Recall" subtitle="Found ALL needed info?\n(Nothing missing?)" icon={Layers} type="secondary" width="w-[160px]" />
    <Node top={75} left={75} title="4. Context Precision" subtitle="Found ONLY relevant info?\n(No garbage chunks?)" icon={Crosshair} type="secondary" width="w-[160px]" />
  </div>
);

// Card 3: Generation (Faithfulness & Relevancy)
const CardGenerationMetrics = () => (
  <div className="relative w-full h-full">
    <div className="absolute top-[8%] left-[25%] -translate-x-1/2 text-xs font-bold text-slate-500 uppercase tracking-widest text-center">Faithfulness</div>
    <div className="absolute top-[8%] left-[75%] -translate-x-1/2 text-xs font-bold text-slate-500 uppercase tracking-widest text-center">Answer Relevancy</div>
    <div className="absolute top-[15%] bottom-[5%] left-[50%] border-l-2 border-dashed border-slate-200"></div>

    <SvgCanvas>
      {/* Faithfulness */}
      <Connection x1={25} y1={25} x2={25} y2={45} arrow={false} />
      <Connection x1={25} y1={45} x2={25} y2={65} />
      {/* Relevancy */}
      <Connection x1={75} y1={25} x2={75} y2={45} arrow={false} />
      <Connection x1={75} y1={45} x2={75} y2={65} />
    </SvgCanvas>

    {/* Faithfulness */}
    <Node top={25} left={25} title="Context vs Answer" subtitle="Context: 'Founded 1998'\nAnswer: 'CEO is John'" type="secondary" width="w-[170px]" />
    <Node top={45} left={25} title="LLM Judge Check" subtitle="Is CEO mentioned in context?" icon={Brain} type="primary" width="w-[160px]" />
    <Node top={65} left={25} title="Score: 0.0 (Fail)" subtitle="Hallucination detected!" icon={XCircle} type="error" width="w-[160px]" />

    {/* Relevancy */}
    <Node top={25} left={75} title="Query vs Answer" subtitle="Q: 'What does company do?'\nA: 'Founded in 1950'" type="secondary" width="w-[170px]" />
    <Node top={45} left={75} title="LLM Judge Check" subtitle="Does '1950' answer 'what'?" icon={Brain} type="primary" width="w-[160px]" />
    <Node top={65} left={75} title="Score: 0.2 (Low)" subtitle="Off-topic answer!" icon={AlertTriangle} type="warning" width="w-[160px]" />
  </div>
);

// Card 4: Retrieval (Recall vs Precision)
const CardRetrievalMetrics = () => (
  <div className="relative w-full h-full flex items-center justify-center">
    <div className="w-[90%] grid grid-cols-2 gap-6 z-10">
      
      {/* Context Recall */}
      <div className="bg-slate-50 border border-slate-200 p-5 rounded-2xl shadow-sm flex flex-col items-center text-center">
        <div className="font-extrabold text-slate-800 text-sm mb-2 flex items-center gap-2">
          <Layers className="text-blue-500" /> Context Recall
        </div>
        <p className="text-[10px] text-slate-500 mb-4 uppercase font-bold tracking-wider">Did we find ALL the pieces?</p>
        
        <div className="w-full flex justify-between items-center mb-4">
          <div className="text-xs">
            <span className="font-bold">Needed:</span><br/>[Q3 Data, Q4 Data]
          </div>
          <ArrowDownUp size={16} className="text-slate-300 rotate-90" />
          <div className="text-xs">
            <span className="font-bold">Retrieved:</span><br/>[Q3 Data]
          </div>
        </div>
        
        <div className="bg-[#ffe4e6] text-[#e11d48] border border-[#fda4af] px-3 py-1.5 rounded-lg text-xs font-bold w-full">
          ❌ Missing Q4! Score: 0.5
        </div>
      </div>

      {/* Context Precision */}
      <div className="bg-slate-50 border border-slate-200 p-5 rounded-2xl shadow-sm flex flex-col items-center text-center">
        <div className="font-extrabold text-slate-800 text-sm mb-2 flex items-center gap-2">
          <Crosshair className="text-blue-500" /> Context Precision
        </div>
        <p className="text-[10px] text-slate-500 mb-4 uppercase font-bold tracking-wider">Are chunks ACTUALLY relevant?</p>
        
        <div className="w-full flex justify-between items-center mb-4">
          <div className="text-xs">
            <span className="font-bold">Query:</span><br/>"Return Policy"
          </div>
          <ArrowDownUp size={16} className="text-slate-300 rotate-90" />
          <div className="text-xs">
            <span className="font-bold">Retrieved:</span><br/>[Policy, CEO Bio, Office]
          </div>
        </div>
        
        <div className="bg-[#fef3c7] text-[#b45309] border border-[#fcd34d] px-3 py-1.5 rounded-lg text-xs font-bold w-full">
          ⚠️ 2/3 Garbage! Score: 0.33
        </div>
      </div>

    </div>
  </div>
);

// Card 5: Diagnostic Guide & Action Plan
const CardDiagnostics = () => (
  <div className="relative w-full h-full flex flex-col items-center p-4">
    
    <div className="w-[95%] bg-white border border-slate-200 rounded-xl overflow-hidden shadow-sm">
      {/* Header */}
      <div className="bg-slate-800 text-white p-3 text-xs font-bold flex items-center gap-2">
        <Stethoscope size={16} /> RAGAS Diagnostic Action Plan
      </div>

      {/* Rows */}
      <div className="divide-y divide-slate-100">
        
        <div className="flex p-3 items-center hover:bg-slate-50">
          <div className="w-1/3 flex items-center gap-2">
            <div className="w-3 h-3 rounded-full bg-red-500"></div>
            <span className="font-bold text-xs text-slate-800">Low Faithfulness</span>
          </div>
          <div className="w-2/3 text-[11px] text-slate-600">
            <span className="font-bold text-slate-800">Issue:</span> LLM is hallucinating answers.<br/>
            <span className="font-bold text-[#31a67a]">Fix:</span> Stricter prompts ("Answer ONLY using context"), use a smarter LLM model.
          </div>
        </div>

        <div className="flex p-3 items-center hover:bg-slate-50">
          <div className="w-1/3 flex items-center gap-2">
            <div className="w-3 h-3 rounded-full bg-orange-500"></div>
            <span className="font-bold text-xs text-slate-800">Low Answer Relevancy</span>
          </div>
          <div className="w-2/3 text-[11px] text-slate-600">
            <span className="font-bold text-slate-800">Issue:</span> Generated answer is off-topic.<br/>
            <span className="font-bold text-[#31a67a]">Fix:</span> Add Few-Shot examples to prompt showing the exact expected output format.
          </div>
        </div>

        <div className="flex p-3 items-center hover:bg-slate-50">
          <div className="w-1/3 flex items-center gap-2">
            <div className="w-3 h-3 rounded-full bg-amber-400"></div>
            <span className="font-bold text-xs text-slate-800">Low Context Recall</span>
          </div>
          <div className="w-2/3 text-[11px] text-slate-600">
            <span className="font-bold text-slate-800">Issue:</span> Failing to retrieve all necessary pieces.<br/>
            <span className="font-bold text-[#31a67a]">Fix:</span> Query rewriting, Multi-query retrieval, or increasing `top_k`.
          </div>
        </div>

        <div className="flex p-3 items-center hover:bg-slate-50">
          <div className="w-1/3 flex items-center gap-2">
            <div className="w-3 h-3 rounded-full bg-yellow-400"></div>
            <span className="font-bold text-xs text-slate-800">Low Context Precision</span>
          </div>
          <div className="w-2/3 text-[11px] text-slate-600">
            <span className="font-bold text-slate-800">Issue:</span> Retrieving too much irrelevant garbage.<br/>
            <span className="font-bold text-[#31a67a]">Fix:</span> Add a Reranker (Cross-Encoder), or improve chunking strategy.
          </div>
        </div>

        <div className="flex p-3 items-center bg-[#e6fcf5]">
          <div className="w-1/3 flex items-center gap-2">
            <div className="w-3 h-3 rounded-full bg-green-500"></div>
            <span className="font-bold text-xs text-[#1f7351]">All Scores &gt; 0.80</span>
          </div>
          <div className="w-2/3 text-[11px] text-[#2a5d48] font-bold">
            System is excellent! Validate with Human Spot-Checks.
          </div>
        </div>

      </div>
    </div>
  </div>
);


// --- MAIN APP LAYOUT ---
export default function App() {
  return (
    <div className="min-h-screen bg-slate-50 p-6 md:p-10 font-sans text-slate-800">
      
      <header className="max-w-7xl mx-auto mb-10 border-b border-slate-200 pb-6 flex flex-col md:flex-row md:justify-between md:items-end gap-4">
        <div>
          <h1 className="text-3xl font-extrabold tracking-tight text-slate-900 flex items-center gap-2">
             Visual Dev Guides <span className="text-slate-400 font-normal">| RAG Evaluation</span>
          </h1>
          <p className="text-slate-500 mt-2 text-sm max-w-lg">
            Phase 1 Theory: Using the RAGAS framework (LLM-as-a-Judge) to mathematically score Faithfulness, Relevancy, Recall, and Precision.
          </p>
        </div>
        <div className="bg-slate-800 text-white text-sm font-bold py-2 px-5 rounded-full shadow-md flex items-center gap-2">
          <BarChart size={16} /> Metrics & Testing
        </div>
      </header>
      
      <main className="max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-2 gap-x-8 gap-y-10">
         <Card title="01. The Evaluation Problem">
           <CardProblem />
         </Card>

         <Card title="02. The 4 RAGAS Metrics">
           <CardRagasOverview />
         </Card>

         <Card title="03. Generation (Faithfulness vs Relevancy)" tall={true}>
           <CardGenerationMetrics />
         </Card>

         <Card title="04. Retrieval (Recall vs Precision)" tall={true}>
            <CardRetrievalMetrics />
        </Card>
      </main>

      <div className="max-w-7xl mx-auto mt-6">
        <Card title="05. RAGAS Diagnostic Action Plan" xTall={true}>
            <CardDiagnostics />
        </Card>
      </div>

    </div>
  );
}